class ExpensesItemModel {
  final String title, date, price, icon;

  ExpensesItemModel(
      {required this.title,
      required this.date,
      required this.price,
      required this.icon});
}
